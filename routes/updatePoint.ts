import { Router } from 'express';

const router = Router();

/*
UPDATE POINT.
*/
router.get('/',() => {});
router.post('/',() => {});
router.get('/:id',() => {});
router.put('/:id',() => {});
router.delete('/:id',() => {});

export default router;